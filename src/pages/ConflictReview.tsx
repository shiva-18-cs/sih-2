import React from 'react';
import ValidationPage from './ValidationPage';

// ConflictReview provides the same validation & conflict review interface
export default function ConflictReview() {
  return <ValidationPage />;
}
